name: minx-browse-mode
root: ~/dev/StitchFix/minx-browse-mode/cra
windows:
  - 'vim':
      panes:
        - nvim -S ~/.local/share/nvim/sessions/minx-browse-mode.vim
  - 'zsh':
      layout: even-horizontal
      panes:
        - yarn install && yarn start
        - clear
