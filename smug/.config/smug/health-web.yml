# root: ~/Developer/galileo-inc/healthweb
session: ${session}
root: ${root}

before_start:
  - source .cypress_local

windows:
  - name: web/nvim
    commands:
      - nvim

  - name: web/server
    commands:
      - PORT=$(port-hash "${session}" dev) yarn start
