session: healthweb
root: ~/Developer/galileo-inc/healthweb/

before_start:
  - source .cypress_local

windows:
  - name: web/nvim
    commands:
      - nvim

  - name: web/server
    commands:
      - yarn start
