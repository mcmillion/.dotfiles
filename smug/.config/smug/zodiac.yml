# root: ~/Developer/galileo-inc/frontend-monorepo
session: ${session}
root: ${root}

windows:
  - name: nvim
    commands:
      - nvim

  - name: claude
    commands:
      - claude

  - name: shell
