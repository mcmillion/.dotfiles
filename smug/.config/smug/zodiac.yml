session: zodiac
root: ~/Developer/galileo-inc/frontend-monorepo

windows:
  - name: nvim
    commands:
      - nvim

  - name: claude
    commands:
      - claude --add-dir ../api --add-dir ../core-api

  - name: server
    commands:
      - nr start
