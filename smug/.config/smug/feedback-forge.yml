# root: ~/Developer/BitPivot/feedback-forge
session: ${session}
root: ${root}

windows:
  - name: nvim
    commands:
      - nvim

  - name: claude
    commands:
      - claude

  - name: server
    commands:
      - PORT=$(port-hash "${session}" dev) bin/dev
