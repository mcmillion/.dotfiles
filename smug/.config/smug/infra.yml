# root: ~/Developer/mcmillion/infra
session: ${session}
root: ${root}

windows:
  - name: nvim
    commands:
      - nvim

  - name: claude
    commands:
      - claude

  - name: maridia
    commands:
      - ssh maridia
