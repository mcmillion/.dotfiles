# root: ~/Developer/BitPivot/desolate.gg
session: ${session}
root: ${root}

windows:
  - name: nvim
    commands:
      - nvim

  - name: claude
    commands:
      - claude

  - name: servers
    commands:
      - PORT=$(port-hash "${session}" dev) nr dev
