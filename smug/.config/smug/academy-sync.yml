session: as
root: ~/Developer/AcademySync/

windows:
  - name: frontend - nvim
    root: frontend/
    commands:
      - nvim

  - name: frontend - server
    root: frontend/
    commands:
      - npm run start

  - name: frontend - xstate
    root: frontend/
    commands:
      - npm run xstate:typegen

  - name: api - nvim
    root: api/
    commands:
      - nvim

  - name: api - server
    root: api/
    commands:
      - bin/rails server
