#==============================================================================
# LINUX-SPECIFIC LOGIN ENVIRONMENT (sourced by ~/.zprofile on Linux)
# Guarded (via _pp from .zprofile) to avoid duplicating the brew path that
# .zshenv already set.
#==============================================================================

_pp /home/linuxbrew/.linuxbrew/bin
